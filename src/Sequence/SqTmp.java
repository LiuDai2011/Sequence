package Sequence;

import arc.struct.ObjectMap;
import mindustry.entities.bullet.BulletType;

public class SqTmp {
    public static final ObjectMap<BulletType, ObjectMap<Float, BulletType>> damageMultiMap = new ObjectMap<>();
}
